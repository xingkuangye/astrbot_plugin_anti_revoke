{
  "monitor_groups": {
      "type": "list",
      "description": "要监控撤回事件的群号列表"
    },
    "target_receivers": {
      "type": "list",
      "description": "接收撤回提醒的目标QQ号"
    },
    "target_groups": {
      "type": "list",
      "description": "接收撤回提醒的目标群号"
    },
    "ignore_senders": {
      "type": "list",
      "description": "忽略这些QQ号的撤回消息"
    },
    "cache_expiration_time": {
      "type": "int",
      "default": 300,
      "description": "消息缓存时间，单位: 秒 (默认5分钟)"
    },
    "file_size_threshold_mb": {
      "type": "int",
      "default": 300,
      "description": "视频和文件大小阈值，单位: MB。超过此大小的视频和文件将不会被缓存 (设为0表示不限制)"
    },
    "forward_relay_group": {
      "type": "string",
      "default": "",
      "description": "合并转发中转群号。填写后，收到的合并转发消息会自动转发到此群，撤回时再转发给接收者。留空则保持原有提示"
    },
    "auto_recall_relay": {
      "type": "bool",
      "default": true,
      "description": "是否在缓存过期后自动撤回中转群的消息 (建议将Bot设置为管理员)"
    }
}
